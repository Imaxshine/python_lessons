def main():
    # Iteratin list by using a while loop
    my_list = [23,34,60,41]
    # First we nned to know that first data is in 
    # index 0 followed by index 1

    # create variable which access first element from the
    # list given

    index = 0
    # Let now apply a while loop 
    while index < 4:
        result = my_list[index]
        print(result)
        index = index + 1
main()